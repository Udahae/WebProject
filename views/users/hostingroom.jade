.container
    h1.page-header List
    
    table.table.table-bordered
      thead
        tr
          th 제목
          th 도시
          th 주소
          th 요금
          th 날짜
          th
      tbody
        each room in rooms
          tr
            td 
              a(href='/rooms/detail/#{room._id}')= room.title
            td= room.city
            td= room.address
            td= room.pay
            td= moment(room.createdAt).format('YYYY-MM-DD')
            td
              a.btn.btn-xs.btn-default(href='/rooms/#{room._id}/edit') 수정
              | 
              a.btn.btn-xs.btn-default(href='/rooms/#{room._id}?_method=DELETE') 삭제
